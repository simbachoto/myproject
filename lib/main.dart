
import'package:flutter/material.dart';
import 'package:gigabyte_ticket_system/home.dart';

void main() => runApp(MyApp(

));

class MyApp extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
   return MaterialApp(
     home: HomePage(),
   );
  }

}

